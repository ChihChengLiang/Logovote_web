version: "2"
services:
  logovote-client:
    build:
      context: ./client
      args:
        ROOT_URL: 0.0.0.0
    ports:
      - 5000:5000
    restart: on-failure
  logovote-server:
    build: ./server
    ports:
      - 3000:3000
    environment:
      - FULLNODE_URL=fullnode
      - NODE_ENV=production
    links:
      - fullnode
    restart: on-failure
  fullnode:
    image: parity/parity:beta
    volumes:
      - ./mnt/:/root/.local/share/io.parity.ethereum/
    command: "--no-ui --no-ancient-blocks --jsonrpc-cors '*' --jsonrpc-interface 'all' --jsonrpc-hosts 'all'"
    restart: on-failure